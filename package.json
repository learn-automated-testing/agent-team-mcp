{
  "name": "agent-team-mcp",
  "version": "0.12.0",
  "description": "Stdio MCP server that inspects a project and installs an agent-orchestrated team (agents + capability skills + workflows) for AI coding assistants. Today targets Claude Code's `.claude/` layout; designed so additional hosts (Copilot, Gemini) can be added as emit targets.",
  "type": "module",
  "bin": {
    "agent-team-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "templates"
  ],
  "scripts": {
    "build": "tsc",
    "postbuild": "chmod +x dist/index.js",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
